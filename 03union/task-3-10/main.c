#include <stdio.h>
#include <stdlib.h>

#include "figure.h"

int main(void) {
    // struct figure_t figures[] = { { .type = CIRCLE, .circ = { .c = { 10, -4 }, .r = 6.621597429597 } }, { .type = TRIANGLE, .triangle = { .p1 = { -19, -20 }, .p2 = { 17, 8 }, .p3 = { -7, -11 } } }, { .type = RECTANGLE, .rect = { .p = { 4, -9 }, .width = 18, .height = 45 } } };
    // struct figure_t *ptr[3];
    //
    // for (int i = 0; i < 3; ++i)
    //     ptr[i] = &figures[i];
    //
    // printf("------------TABLICA FIGUR DO POSORTOWANIA-----------\n");
    //
    // for (int i = 0; i < 3; ++i)
    // {
    //     printf("%.4f ", area_figure(figures  + i));
    //     display_figure(figures  + i);
    // }
    //
    // printf("------------TABLICA FIGUR PO SORTOWANIU-----------\n");
    //
    // // qsort(ptr, 3, sizeof(ptr[0]), compare_figures_by_area);
    // sort_by_area(ptr, 3);
    //
    // // test_error(res == 0, "Funkcja sort_by_area powinna zwrócić %d, a zwróciła %d", 0, res);
    //
    // struct figure_t expected_figures[] = { { .type = RECTANGLE, .rect = { .p = { 4, -9 }, .width = 18, .height = 45 } }, { .type = CIRCLE, .circ = { .c = { 10, -4 }, .r = 6.621597429597 } }, { .type = TRIANGLE, .triangle = { .p1 = { -19, -20 }, .p2 = { 17, 8 }, .p3 = { -7, -11 } } } };
    //
    // for (int i = 0; i < 3; ++i)
    // {
    //     printf("%d %.4f ", i, area_figure(ptr[i]));
    //     display_figure(ptr[i]);
    // }
    //
    // printf("------------OCZEKIWANA TABLICA FIGUR-----------\n");
    //
    // for (int i = 0; i < 3; ++i)
    // {
    //     printf("%d %.4f ", i, area_figure(expected_figures + i));
    //     display_figure(expected_figures  + i);
    // }
    //
    // printf("------------\n");


    return 0;
}